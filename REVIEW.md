# راهنمای بررسی تغییرات

> این فایل برای مرور کارهای انجام‌شده روی این برنچ نوشته شده، نه برای مستندات دائمی پروژه.
> بعد از merge می‌توانید حذفش کنید.

| | |
|---|---|
| برنچ بک‌اند | `feat/drive-backup-demo-tree-excel-edits` (۷ کامیت) |
| برنچ فرانت | `feat/landing-faq-and-public-demo` (۳ کامیت) |
| وضعیت | هیچ‌کدام push نشده‌اند |
| تاریخ | ۱۴۰۵/۰۷/۰۱ — 2026-09-23 |

نسخهٔ یکسانی از این فایل در ریپوی بک‌اند هم هست. لینک‌های فایل‌های بک‌اند در این نسخه
ساده‌متن‌اند، چون در ریپوی دیگری قرار دارند.

---

## فهرست

- [۱. چه چیزهایی تغییر کرد](#۱-چه-چیزهایی-تغییر-کرد)
- [۲. تصمیم‌هایی که با شماست](#۲-تصمیمهایی-که-با-شماست)
- [۳. بررسی بکاپ آف‌سایت (گنجینه آروان)](#۳-بررسی-بکاپ-آفسایت-گنجینه-آروان)
- [۴. بررسی ویرایش با اکسل](#۴-بررسی-ویرایش-با-اکسل)
- [۵. بررسی دموی عمومی](#۵-بررسی-دموی-عمومی)
- [۶. بررسی سئو و متن‌ها (فرانت)](#۶-بررسی-سئو-و-متنها-فرانت)
- [۷. چه چیزی اجرا شد و چه چیزی نشد](#۷-چه-چیزی-اجرا-شد-و-چه-چیزی-نشد)
- [۸. مواردی که از قبل خراب بودند](#۸-مواردی-که-از-قبل-خراب-بودند)

---

## ۱. چه چیزهایی تغییر کرد

سه قابلیت، در دو ریپو:

| # | قابلیت | بک‌اند | فرانت |
|---|--------|--------|-------|
| ۱ | بکاپ شبانه روی گنجینه آروان (S3) | ✅ | — |
| ۲ | ایمپورت اکسل که ویرایش هم می‌کند | ✅ | — |
| ۳ | شجره‌نامهٔ دموی عمومی | ✅ | ✅ |
| ۴ | بخش سوالات پرتکرار + بازنویسی متن فارسی | — | ✅ |

**هر سه قابلیت پیش‌فرض خاموش یا بی‌اثرند.** بدون `OFFSITE_BACKUP_ENABLED` و بدون `DEMO_TREE_ID`،
رفتار سیستم دقیقاً مثل قبل است. تنها تغییری که بلافاصله اثر دارد، ایمپورت اکسل است.

### کامیت‌های بک‌اند

```
8eb1bfa  chore(deps)       افزودن کتابخانه‌های Google API
be44fe1  feat(infra)       آپلود بکاپ شبانه روی Drive
398974f  docs(docs)        مستندات بکاپ Drive
d378b40  feat(application) ایمپورت اکسل: ردیف تطبیق‌خورده = ویرایش
8b7b1d9  feat(presentation) دموی عمومی فقط-خواندنی
7cf3280  fix(ci)           نوشتن خروجی OpenAPI با UTF-8
392db37  chore(api)        بازتولید openapi.json و کلاینت
```

### کامیت‌های فرانت

```
59aa92e  feat(landing)  بخش سوالات پرتکرار + JSON-LD
550f358  ref(i18n)      بازنویسی متن‌های فارسی صفحهٔ اصلی
3e9197a  feat(pedigree) صفحهٔ دموی عمومی
```

---

## ۲. تصمیم‌هایی که با شماست

این‌ها را عمداً خودم تصمیم نگرفتم. اگر با هیچ‌کدام موافق نبودید، بگویید تا عوض کنم.

### ۲.۱ خروجی اکسل درخت دمو عمومی می‌شود

`GET /excel/export` و `GET /excel/sample` فقط مجوزهای خواندنی می‌خواهند، پس با فعال‌شدن دمو
**هر کسی می‌تواند کل درخت دمو را به‌صورت اکسل دانلود کند.**

برای دادهٔ عمومی احتمالاً اشکالی ندارد و حتی دموی خوبی است. اگر نمی‌خواهید، راهش این است که در
`tree_excel_router.py` این دو روت به‌جای
`require_tree_view` از `require_tree_member` استفاده کنند.

### ۲.۲ محتوای درخت دمو ایندکس می‌شود

صفحهٔ `/demo` در فرانت `index: true` دارد و در sitemap هست. یعنی اسم و تاریخ تولد افراد داخل آن
درخت در گوگل قابل جستجو می‌شود. **اسم‌های واقعی فامیل خودتان را در درخت دمو نگذارید.**

### ۲.۳ ایمپورت اکسل هیچ‌وقت مقدار را پاک نمی‌کند

سلول خالی مقدار ذخیره‌شده را پاک نمی‌کند، و روابط پدر/مادر و ازدواجِ افرادِ موجود بازنویسی نمی‌شوند.

دلیل: یک شیت دست‌ساز ممکن است اصلاً آن ستون را نداشته باشد؛ پاک‌شدن بی‌صدای محل تولد، یا جدا شدن
آدم‌ها از والدینشان، قیمت سنگینی برای راحتیِ «حذف از اکسل» است.

نتیجهٔ عملی: حذف مقدار و تغییر رابطه فقط در خود برنامه ممکن است. اگر می‌خواهید از اکسل هم بشود،
باید یک ستون صریح مثل `clear` تعریف کنیم — حدس‌زدن از روی سلول خالی خطرناک است.

### ۲.۴ همین فایل `REVIEW.md`

در ریپوی عمومی کامیت شده و فارسی است، درحالی‌که بقیهٔ مستندات انگلیسی‌اند. اگر نمی‌خواهید در ریپو
بماند بگویید تا حذفش کنم یا به `.gitignore` اضافه‌اش کنم.

---

## ۳. بررسی بکاپ آف‌سایت (گنجینه آروان)

### چه شد

Google Drive حذف شد. بکاپ آف‌سایت حالا با `backup.upload_offsite` به باکت S3-compatible
(گنجینه ابر آروان) می‌رود. جزئیات و چک‌لیست در `OFFSITE-BACKUP.md` ریپوی بک‌اند است.

```
backups/2026/2026-09/2026-09-23_00-00-00/
    postgres_family_tree_2026-09-23_00-00-00.dump
    neo4j_2026-09-23_00-00-00.zip
    manifest.json
```

### چه چیزی را بررسی کنید

- [ ] راه‌اندازی باکت + کلید طبق `OFFSITE-BACKUP.md`
- [ ] یک اجرای دستی و دیدن لاگ `Uploaded backup … to off-site prefix …`
- [ ] یک بازیابی واقعی از باکت
- [ ] `OFFSITE_BACKUP_RETENTION_DAYS` با سهمیهٔ پلن رایگان

---

## ۴. بررسی ویرایش با اکسل

### مشکلی که وجود داشت

ایمپورت **فقط ایجاد می‌کرد**. ردیفی که با فرد موجود تطبیق می‌خورد کلاً رد می‌شد:

- ردیف با `system_id` (خروجی خود سیستم) → با UUID تطبیق می‌خورد، «موجود» شناخته می‌شد، **بی‌صدا رد
  می‌شد** و نام خانوادگی اصلاح‌شده هیچ‌وقت نوشته نمی‌شد.
- ردیف بدون `system_id` → کلید هویت شامل نام خانوادگی است، پس با تغییر فامیلی تطبیق نمی‌خورد و
  **یک فرد تکراری ساخته می‌شد**.

### چه شد

| فایل | نقش |
|---|---|
| `tree_excel_service.py` | `person_row_changes` و `apply_person_row_changes` — منبع مشترک |
| `import_tree_excel_use_case.py` | حلقهٔ آپدیت، سینک به Neo4j، `persons_updated` |
| `preview_tree_excel_use_case.py` | `changed_fields` + اعتبارسنجی ویرایش روی یک کپی |

پیش‌نمایش و ایمپورت **یک تابع مشترک** را صدا می‌زنند، پس چیزی که پیش‌نمایش قول می‌دهد همان است که
ایمپورت انجام می‌دهد.

### چه چیزی را بررسی کنید

- [ ] خروجی اکسل بگیرید، یک نام خانوادگی را عوض کنید، برگردانید → باید **ویرایش** شود، نه تکراری
- [ ] همان کار روی شیتی **بدون** ستون `system_id` → این حالت هنوز فرد جدید می‌سازد (چون راهی برای
      تشخیص هویت نیست). مطمئن شوید این رفتار برایتان قابل قبول است.
- [ ] یک ستون را کلاً از شیت حذف کنید → هیچ مقداری نباید پاک شود
- [ ] تاریخ فوت قبل از تولد بگذارید → باید در **پیش‌نمایش** خطا بدهد، نه موقع ثبت
- [x] UI فرانت: فیلد جدید `changed_fields` در پیش‌نمایش و `persons_updated` در پاسخ ایمپورت
      (`ExcelPreviewDialog` / `excel-import` / پیام موفقیت).

---

## ۵. بررسی دموی عمومی

### ایدهٔ معماری

به‌جای ساختن یک روتر `/demo` با کپی روت‌های خواندنی (که قرارداد API را دو نسخه می‌کرد)، دمو در
همان لایه‌ای گذاشته شد که از قبل تصمیم می‌گیرد چه کسی چه می‌بیند: **مجوزها**.

بازدیدکنندهٔ بی‌حساب یک **عضویت مصنوعی** می‌گیرد با فقط چهار مجوز:
`view`، `view_birth_date`، `view_marriage_date`، `view_photo`.

از آن به بعد هیچ‌جای کد نمی‌داند «این یک بازدیدکنندهٔ دمو است» — فقط عضوی با دسترسی محدود می‌بیند.
یعنی **هیچ روتی جداگانه امن‌سازی نشد**، و روت نوشتنیِ جدیدی که فردا اضافه شود خودبه‌خود بسته است.

### سطح دسترسی واقعی

| عمومی شد (۸ روت) | عضو-فقط ماند (۱۳ روت) |
|---|---|
| `POST …/persons/list` | `POST/PUT/DELETE …/persons` |
| `GET …/persons/{id}` | `POST/PUT/DELETE …/marriages` |
| `GET …/relation/{a}/{b}` | `POST …/marriages/divorce` |
| `GET …/relation/{a}/{b}/alternatives` | `POST …/media/upload` |
| `POST …/marriages/list` | `POST …/excel/import` و `/preview` |
| `GET …/marriages/{id}` | `POST/PATCH/DELETE …/members` |
| `GET …/excel/sample` | |
| `GET …/excel/export` | |

### دو مرز مهم

1. **۴۰۱ حفظ شد.** درخواست بدون توکن به چیزی که عمومی نیست، همچنان ۴۰۱ می‌گیرد نه ۴۰۳. انتشار یک
   درخت نباید خطای احراز هویت را بی‌صدا به خطای دسترسی تبدیل کند.
2. **توکن خراب ≠ ناشناس.** فقط *نبودن* هدر `Authorization` یعنی ناشناس. توکن منقضی همچنان خطا
   می‌دهد، وگرنه کاربری که سشنش تمام شده بی‌خبر نمای عمومی را می‌دید.

### چه چیزی را بررسی کنید

- [ ] یک درخت نمونه بسازید و آیدی‌اش را در `DEMO_TREE_ID` بگذارید
- [ ] در مرورگر ناشناس `/fa/demo` و `/en/demo` را باز کنید
- [ ] **مطمئن شوید هیچ دکمهٔ ویرایش/افزودن/حذفی دیده نمی‌شود** (باید خودبه‌خود غایب باشند)
- [ ] با `curl` یک روت نوشتنی روی درخت دمو را بدون توکن صدا بزنید → باید ۴۰۱ بدهد
- [ ] با توکن یک کاربر دیگر (غیرعضو) همان روت نوشتنی → باید ۴۰۳ بدهد
- [x] مسیریابی نسبت بین دو نفر در دمو کار می‌کند؟
      *(باگ: `runRelation`/`useExcelTransfer` کلید UI `"demo"` را به API
      می‌فرستادند؛ الان از `tree.treeMeta.id` استفاده می‌شود.)*
- [ ] `DEMO_RATE_LIMIT_PER_MINUTE` (پیش‌فرض ۶۰) — توجه کنید روتی مثل `excel/export` سه گارد دارد
      و هر بار **سه واحد** از سهمیه مصرف می‌کند
- [ ] عکس افراد در دمو از MinIO لود می‌شود؟ (presigned URL کوتاه‌مدت)

### تست‌ها

`tests/unit/services/test_tree_demo_access.py` —
۲۲ تست که مرز را قفل می‌کنند، از جمله پارامتریک روی **تک‌تک** مجوزهای `MANAGEMENT`. اگر کسی فردا
مجوز نوشتنی به تاپل `DEMO` اضافه کند، این تست‌ها می‌شکنند.

---

## ۶. بررسی سئو و متن‌ها (فرانت)

### سوالات پرتکرار

- ۱۲ سوال فارسی و ۱۲ سوال انگلیسی، با **مجموعهٔ متفاوت** — چون نیت جستجو در دو زبان فرق دارد.
  فارسی از «چطور شروع کنم» و «خصوصی می‌ماند؟» شروع می‌شود، انگلیسی از GEDCOM، self-hosting و
  «second cousin once removed».
- کامپوننت کلیدها را از فایل زبانِ همان locale می‌خواند، پس هر زبان می‌تواند تعداد و ترتیب خودش را
  داشته باشد.
- با `<details>` بومی رندر می‌شود: متن پاسخ‌ها همیشه در DOM است (خزنده می‌خواندشان)، بدون هیچ
  جاوااسکریپت کلاینتی.
- JSON-LD نوع `FAQPage` از **همان پیام‌ها** ساخته می‌شود تا با متن نمایشی اختلاف پیدا نکند.

### چه چیزی را بررسی کنید

- [ ] صحت جواب‌ها — من با کد تطبیقشان دادم (مثلاً سقف حساب رایگان از `account_type.py`)، ولی شما
      محصول را بهتر می‌شناسید
- [ ] پاسخ GEDCOM صادقانه نوشته شده: «فعلاً پشتیبانی نمی‌شود، اکسل جایگزین آن است». اگر برنامه‌ای
      برای GEDCOM دارید، متن باید عوض شود
- [ ] پاسخ open-source فقط دربارهٔ **بک‌اند** ادعای MIT می‌کند، چون ریپوی فرانت فایل `LICENSE`
      ندارد. **اگر فرانت هم اوپن‌سورس است، یک `LICENSE` اضافه کنید** تا متن را کامل‌تر کنم
- [ ] متن‌های بازنویسی‌شدهٔ فارسی صفحهٔ اصلی را یک بار بخوانید
- [ ] بعد از دیپلوی: تست با Google Rich Results برای `FAQPage`
- [ ] بعد از دیپلوی: ثبت `sitemap.xml` در Search Console (حالا `/demo` هم داخلش هست)

---

## ۷. چه چیزی اجرا شد و چه چیزی نشد

### اجرا شد ✅

| | بک‌اند | فرانت |
|---|---|---|
| Lint | `ruff check app tests scripts` | `eslint` (۰ خطا، ۳ warning از قبل موجود) |
| Types | `mypy` — ۳۰۷ فایل، بدون خطا | `tsc --noEmit` |
| Format | `ruff format` | `prettier` از طریق lint-staged |
| تست | ۴۷۸ تست واحد (۲۹ تای جدید) | ۱۱۳ تست |
| Build | — | `next build` |
| Drift | openapi + client + `.env.example` + requirements | — |

### اجرا نشد ⚠️

- **تست‌های e2e بک‌اند** — به Postgres، Neo4j، Redis و MinIO زنده نیاز دارند. این مهم‌ترین چیزی است
  که باید خودتان اجرا کنید، مخصوصاً برای ایمپورت اکسل.
- **آپلود واقعی روی گنجینه آروان** — باکت و Access Key لازم دارد.
- **بازیابی واقعی از باکت آف‌سایت**.
- **دموی واقعی با یک `DEMO_TREE_ID` تنظیم‌شده** — همهٔ تست‌ها واحد بودند.
- **تست مرورگری صفحهٔ دمو** — فقط HTML رندرشده را با curl بررسی کردم.

### چیزهایی که عمداً انجام ندادم

- فرانت `changed_fields` و `persons_updated` را در پیش‌نمایش/پیام موفقیت نشان می‌دهد (بخش ۴)
- هیچ چیزی push نشده و هیچ PR ساخته نشده
- فایل `.env` محلی ساختم تا تست‌ها اجرا شوند؛ در `.gitignore` هست و کامیت نشده

---

## ۸. مواردی که از قبل خراب بودند

این‌ها ربطی به این تغییرات ندارند، ولی حین کار دیدمشان:

1. **`check_graphql_client_sync.py` شکست می‌خورد** روی `update_ticket_status.py`. فایل کامیت‌شده با
   چیزی که `ariadne-codegen` تولید می‌کند فرق دارد. دست نزدم تا تغییرات نامربوط وارد این برنچ نشود.
   رفعش: `poetry run ariadne-codegen client`.

2. **`export_openapi_schema.py` روی ویندوز فایل را خراب می‌کرد** — `write_text` بدون `encoding`
   از encoding پیش‌فرض سیستم استفاده می‌کرد و هر متن غیرASCII (فارسی یا em dash) فایل را با cp1252
   می‌نوشت. چون مانع کارم بود، این یکی را در کامیت `7cf3280` رفع کردم.

3. سه warning در `eslint` فرانت (`export-e2e.mjs`، `media.ts`، `layout.ts`) که از قبل بودند.
